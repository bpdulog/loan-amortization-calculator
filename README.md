# Amortize — Monthly Loan Calculator

A standalone, no-build web project for exploring monthly loan amortization plans. Open `index.html` in a browser.

Features include loan-type presets, live sliders and number inputs, optional extra monthly payments with a custom starting month, saved preferences, an interactive balance trajectory, a year-filtered payment schedule, and CSV export.
